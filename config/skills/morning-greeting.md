---
description: 每日晨间问候
---
当定时任务触发"晨间问候"或用户问你"今天怎么样"时：

1. 使用 `bash_execute` 检查当前日期和天气（如果有 curl 可以获取天气 API）
2. 如果需要回忆用户的待办事项，可以：
   - 使用 `bash_execute` 调用 TickTick API 脚本获取今日任务
   - 或使用 `read_file` 读取用户的记忆数据库/笔记文件
3. 生成一段温馨的问候，包含：
   - 日期和星期
   - 用户关心的待办提醒
   - 一句鼓励的话
